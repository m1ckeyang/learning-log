# 每日学习日志

## 日期
2026-07-13

## 今日目标
- [x] 完成文件整理机器人 v0.1，能自动按文件类型归类

## 做了什么
- 创建 file-organizer 项目目录，验证 Python 开发链路（python3 main.py 输出成功）
- 编写文件整理机器人 v0.1：按文件扩展名自动分类到「图片」「文档」「视频」「音频」「压缩包」「代码」「其他」子文件夹
- 创建 test-folder 测试目录，放了 8 个不同类型的文件进行测试
- 运行脚本，验证文件自动归类效果
- 完成 Git commit 和 push

## 卡在哪里
- `code .` 命令未配置（zsh: command not found），直接在 VS Code GUI 里操作绕过了这个问题

## 解决了什么问题
- 文件整理脚本从一行 print 发展到能实际工作的分类工具
- SSH 方式连接 GitHub（昨天配置的），今天 push 稳定无报错

## 学到了什么
- Python 核心语法实战：字典（file_categories）、for 循环（os.listdir）、条件判断（if ext in extensions）、函数（get_category / organize_files）
- 文件操作：os.path.splitext 取扩展名、os.makedirs 创建目录、shutil.move 移动文件
- `if __name__ == "__main__":` 的作用——让脚本既能直接运行，又能被其他脚本导入调用

## Git 提交
- 提交了 1 次
- 仓库：m1ckeyang/learning-log
- commit：`feat: 文件整理机器人v0.1 - 按类型自动归类`

## 明天要做的第一件事
- 给文件整理机器人加命令行参数（argparse），不用改代码就能指定不同目录

---

## 心流自评（1-5 分）
- 专注度：3
- 挫败感：2
- 成就感：4

> 今天有明确的任务（写文件整理脚本），代码跑出来有即时反馈（文件自动归类的效果），难度适中（照着框架改而不是从零写）。成就感偏高是因为第一次写出能用的工具。
